console.log(document);
console.dir(document);


// -window is the real top-most object
// -window gives you access to all the 
//  core APIs that the browser wants to expose to you
// -window represents a tab in the browser
console.log(window);

// -document object is a property of window
// -document.xxx is really window.document.xxx
