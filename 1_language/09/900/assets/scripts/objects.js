const person = {
    name: 'josh',
    greet: function () {
        alert('hi from method');
    }
};

// Create object
console.log(person.name);
person.greet();


// Log:
// -1 object
// -1 arrays
// -Legs VERY slightly sore and core not sore at all